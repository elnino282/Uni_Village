import { ChannelInfoScreen } from '@/features/channel';
import React from 'react';

/**
 * Channel Info route - displays group/channel information
 * Route: /channel/[channelId]/info
 */
export default function ChannelInfoRoute() {
    return <ChannelInfoScreen />;
}
