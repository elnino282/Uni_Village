import type { Itinerary } from '../types/itinerary.types';

/**
 * Temporary mock service. Replace with real API calls when backend is ready.
 */
export async function fetchItineraries(): Promise<Itinerary[]> {
    // TODO: swap with real network request
    return [];
}
